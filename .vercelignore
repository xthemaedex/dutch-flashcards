# The deploy is just the static site. Everything the app serves is in public/
# (shell + public/data/*.json + icons). No build step, no functions.
data
db
audio
images
scripts
node_modules
.git
.env
.env.*
*.md
!README.md
